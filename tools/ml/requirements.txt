# Predictive-maintenance training + export pipeline.
# Install into a venv:  python -m venv .venv && .venv/Scripts/activate (Windows)
#                       python -m venv .venv && source .venv/bin/activate (macOS/Linux)
#                       pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
imbalanced-learn>=0.11
joblib>=1.3
